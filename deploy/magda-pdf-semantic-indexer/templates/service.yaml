apiVersion: v1
kind: Service
metadata:
  name: "magda-pdf-semantic-indexer"
spec:
  ports:
  - name: http
    port: 80
    targetPort: {{ .Values.port }}
  selector:
    service: magda-pdf-semantic-indexer